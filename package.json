{
  "name": "jellyfin-customlogo-plugin",
  "version": "2.0.0",
  "description": "Custom Logo plugin for Jellyfin 10.11 and 12.0",
  "private": true,
  "scripts": {
    "build": "node build.js --no-zip",
    "package": "node build.js",
    "deploy": "node build.js"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "archiver": "^6.0.1"
  }
}
